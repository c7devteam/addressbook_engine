<%= form_for account_contact_group, url: address_book_group_update_path, :html => {:class => 'form-horizontal'}, :remote => true do |f| %>

  <div class="control-group">
    <label class="control-label" for="name"><%= t('general.address_book_group.labels.name') %></label>
    <div class="controls">
      <%= f.text_field :name %>
    </div>
  </div>
    <hr/>
  <div class="actions">
    <button type="submit" class="btn btn-primary"><%= t('general.actions.save') %></button>
  </div>
<% end %>